'use strict';

/**
 * landingpage-squeezepage router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::landingpage-squeezepage.landingpage-squeezepage');
