'use strict';

/**
 * landingpage-squeezepage service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::landingpage-squeezepage.landingpage-squeezepage');
