'use strict';

/**
 * landingpage-squeezepage controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::landingpage-squeezepage.landingpage-squeezepage');
